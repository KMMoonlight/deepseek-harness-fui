/** Host loader entry for the desktop installer browser implementation exported from `./client`. */

/** Host plugin body — no host-side behavior for the installer tab. */
export function apply(): void {}
