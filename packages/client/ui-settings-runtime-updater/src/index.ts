/** Host loader entry for the desktop runtime updater browser implementation exported from `./client`. */

/** Host plugin body — no Host behavior belongs to the settings row. */
export function apply(): void {}
